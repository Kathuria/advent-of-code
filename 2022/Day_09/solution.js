const fs = require('fs')

const filename = 'input-sample.txt'
const inputs = fs.readFileSync(filename).toString('utf8')

//Not able to visualize, will try later

console.log('Part 1, How many positions does the tail of the rope visit at least once? =', ); 

//console.log('Part 2,  =', );
